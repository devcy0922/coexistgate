# Roadmap (not V0.1)

Ideas that showed up during bootstrap. **Do not implement these in V0.1.**

- GitLab / Bitbucket adapters
- Python / Go application analyzers
- TypeScript AST (swc/tree-sitter) if regex hits STOP B
- MySQL / Prisma migrate parsers
- Helm rendering
- Optional LLM explainer (never a judge)
- Dashboard, orgs, Slack, hosted SaaS
- Scheduled scanning
- Package registries (brew, npm, crates.io)
- Custom domain beyond GitHub Pages
- Terraform/Checkov-style IaC scanning
